<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jupc.itics.tesoem.edu.proyectojupc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CreadorActivity"></activity>
        <activity android:name=".InterActivity" />
        <activity android:name=".LigaMxActivity" />
        <activity android:name=".MundialActivity" />
        <activity android:name=".LigaIngActivity" />
        <activity android:name=".LigaEspaActivity" />
        <activity android:name=".ConcachampionsActivity" />
        <activity android:name=".LibertadoresActivity" />
        <activity android:name=".EuropaActivity" />
        <activity android:name=".ChampionsActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>